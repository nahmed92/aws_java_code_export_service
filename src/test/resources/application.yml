spring:
  profiles:
    active: test

management:
  endpoint:
    shutdown:
      enabled: true
server:
  port: 0

---
spring:
  profiles: test
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false